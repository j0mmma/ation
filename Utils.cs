using System.Numerics;

using Ation.Common;

namespace Ation.Utils;

static class Utils
{
    public static bool PositionInsideWindow(Vector2 pos) { return true; }
    public static bool PositionInsideWorld(Vector2 pos) { return true; }
}